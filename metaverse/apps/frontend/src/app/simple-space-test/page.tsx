'use client';

import VirtualSpaceCanvas from '../../../components/virtual-space-canvas';

export default function SimpleSpaceTestPage() {
  return (
    <div className="w-full h-screen">
      <VirtualSpaceCanvas />
    </div>
  );
} 